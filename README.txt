Так тут я узнал как собственно спроектировать простенький бекенд сервер
тут не раскрыто как авторизацией управлять ( куки туда сюда) но с jwt разобрался
баззы данных - синтаксис надо изучать тут простая локальная призм(доку тож читать надо)
собственно наверное особо больше ничего и не надо - апи создаю = интерфейс(энтити) => в базу его затянул она там типы создала = > 
создаем сервис для работы с этой энтити - инжектим его в приложение - пишем роуты( работаем с базой ) 
как-то так вроде бы
=====
след задача => пет проект(фронт/бек) - бек пишу как умею + фронт красиво
2. nest.js изучаем
3. db syntaxys
4. doker 
во фронте вроде все намана вот ща надо спроектировать проект большой с 0 самому
